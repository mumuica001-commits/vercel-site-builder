import React from 'react'
import ReactDOM from 'react-dom/client'
import LoteNoturno from './LoteNoturno'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LoteNoturno />
  </React.StrictMode>,
)